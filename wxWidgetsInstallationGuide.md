# wxWidgets Installation Guide #

  * untar, cd into wxFolder
  * ./configure --with-x11 --with-opengl
  * make
  * sudo make install
  * sudo ldconfig

TODO: Will probably also need some other libs (GLEW, GLUT, etc)